#include "GeneticAlg.h"
#include <iostream>
#include <vector>
#include <exception>
#include <time.h>
int main(){
	srand(time(NULL));
	//freopen("output.txt","w",stdout);
    cout<<"write in file";
    //GeneticAlgorithm gen(10);
	return 0;
	
}